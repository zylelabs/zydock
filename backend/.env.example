PORT="8000"
SERVER_IDLE_TIMEOUT_SECONDS="120"

MODE="dev"

LOG_LEVEL="info"

CORS_ORIGIN="http://localhost:3000"
APP_URL="http://localhost:3000"

MONGO_URI=""

JWT_SECRET=""
JWT_ACCESS_TTL_SECONDS="900"
JWT_REFRESH_TTL_DAYS="30"

SUPERUSER_EMAILS=""
PASSWORD_RESET_TTL_MINUTES="30"

# 32 bytes in hex — generate with: openssl rand -hex 32
ENCRYPTION_KEY=""

NODE_AGENT_PORT="9000"
NODE_AGENT_TIMEOUT_MS="15000"
NODE_AGENT_OFFLINE_AFTER_SECONDS="90"
NODE_AGENT_BUNDLE_PATH="../node/dist/agent.js"

CONTAINER_RUNTIME="docker"
REVERSE_PROXY="caddy"
STORAGE_PROVIDER="local"
DNS_PROVIDER="cloudflare"
GIT_DEFAULT_HOST="github"
SSH_PROVIDER="ssh2"
BACKEND_URL="http://localhost:8000"

STORAGE_LOCAL_PATH="./storage"

SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="Zydock <no-reply@localhost>"

QUEUE_ENABLED="true"
QUEUE_POLL_INTERVAL_MS="1000"
QUEUE_CONCURRENCY="2"
QUEUE_MAX_ATTEMPTS="3"
QUEUE_RETRY_DELAY_MS="5000"
QUEUE_JOB_TIMEOUT_SECONDS="1800"

DEPLOY_WORKSPACE_PATH="/var/lib/zydock/builds"
DEPLOY_HEALTHCHECK_TIMEOUT_SECONDS="180"
DEPLOY_BUILD_LOG_LINES="500"

LOGS_STREAM_TAIL="100"

PROXY_NETWORK="zydock"

METRICS_RETENTION_HOURS="168"
METRICS_STREAM_INTERVAL_SECONDS="5"

NOTIFICATIONS_RETENTION_HOURS="720"
